/* install.js - aba "Instalar".
 * Casca vazia: o conteúdo chega na Task 11.
 * O módulo é registrado agora para que tmx.tabs.show() já tenha o que chamar. */
(function () {
  'use strict';
  window.tmxTabs = window.tmxTabs || {};

  window.tmxTabs.instalar = {
    init: function () {
      // Task 8: nada a montar ainda - a seção #tab-instalar já traz o texto
      // "Em construção" direto no HTML.
    }
  };
})();
