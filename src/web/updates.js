/* updates.js - aba "Atualizações".
 * Casca vazia: o conteúdo chega na Task 13.
 * O módulo é registrado agora para que tmx.tabs.show() já tenha o que chamar. */
(function () {
  'use strict';
  window.tmxTabs = window.tmxTabs || {};

  window.tmxTabs.atualizacoes = {
    init: function () {
      // Task 8: nada a montar ainda - a seção #tab-atualizacoes já traz o texto
      // "Em construção" direto no HTML.
    }
  };
})();
