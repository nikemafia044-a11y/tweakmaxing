/* microwin.js - aba "MicroWin".
 * Casca vazia: o conteúdo chega na Task 16.
 * O módulo é registrado agora para que tmx.tabs.show() já tenha o que chamar. */
(function () {
  'use strict';
  window.tmxTabs = window.tmxTabs || {};

  window.tmxTabs.microwin = {
    init: function () {
      // Task 8: nada a montar ainda - a seção #tab-microwin já traz o texto
      // "Em construção" direto no HTML.
    }
  };
})();
